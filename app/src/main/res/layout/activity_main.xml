<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#FFFFFF"
    tools:context="com.example.mayank.linkpreview.MainActivity">

    <include
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        layout="@layout/cardview_layout"
        android:layout_above="@+id/urlLinkTv"
        android:layout_marginBottom="15dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="@string/url_link"
        android:gravity="center"
        android:textColor="@color/black"
        android:inputType="textUri"
        android:id="@+id/urlLinkTv"
        android:imeOptions="actionDone"
        android:layout_centerVertical="true"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/find_preview"
        android:layout_below="@+id/urlLinkTv"
        android:id="@+id/findPreviewButton"
        android:layout_marginTop="20dp"/>



</RelativeLayout>
